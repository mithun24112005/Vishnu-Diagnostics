/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://shreevishnudiagnostics.com",
  generateRobotsTxt: true,
  changefreq: "weekly",
  priority: 0.7,
  sitemapSize: 7000,
};

module.exports = config;
